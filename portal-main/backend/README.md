# Shell backend

Run locally:

```powershell
cd backend
python -m pip install -r requirements.txt
python -m uvicorn main:app --reload
```

Default frontend URL:
- http://127.0.0.1:8000

Use the **Backend URL** field inside Python/Linux/Playbook shell panels if you deploy the backend elsewhere.